versions:
  - date: "2026-01-01"
    status: stable
    changes: []

  - date: "2026-04-11"
    status: latest
    changes:
      - type: additive
        description: "Added meta.is_historical to entity responses"
      - type: additive
        description: >-
          Added X-PII-Masked response header (retired 2026-07-01 — the serving
          tier holds no PII, so the current build never emits it; the name
          stays reserved)
