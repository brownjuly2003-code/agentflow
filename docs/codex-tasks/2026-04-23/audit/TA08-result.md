## Architectural debt inventory (HEAD `a010a2d`)

| # | Item | Impact | Workaround in place | Real fix | Effort | Ticket | Risk if deferred |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | SDK package name collision | Runtime repo and Python SDK share one distribution identity, so editable installs stay order-dependent. | Tests/examples prepend `sdk/` via `sys.path`; setup/docs install `sdk/` separately; dual install is avoided by convention. | Split runtime and SDK package identity, update release/docs/integrations, ship migration plan. | 2-3 weeks (planning) | [A01](../../2026-04-Q2-architecture/A01-sdk-package-identity-split.md) | The next consumer or CI path that installs both runtime and SDK can resolve imports/CLI to the wrong package depending on install order. |
| 2 | Query engine mixin host contracts | Query layer type safety is blind to hidden coupling on host attributes like `_backend`, `catalog`, `_tenant_router`. | `pyproject.toml` disables mypy `attr-defined` for `src.serving.semantic_layer.query.*`. | Replace implicit mixin coupling with Protocol/composition-based contracts and remove the broad mypy suppression. | 1-2 weeks (planning) | [A02](../../2026-04-Q2-architecture/A02-query-engine-mixin-contracts.md) | A future refactor can break query paths only at runtime, and hardening debt will keep reappearing in the same layer. |
| 3 | Entity latency optimization plan drift | Existing perf backlog still points at hypotheses that do not match the current entity hot path. | Current release gate stays `<500 ms`; `scripts/profile_entity.py` and workflow repair tickets provide only tactical validation. | Re-baseline on a representative stack, re-profile entity lookup, then retarget optimizations to the measured bottleneck. | 1-2 weeks (planning) | [A03](../../2026-04-Q2-architecture/A03-entity-latency-rebaseline.md) | A sprint can be spent on no-op optimizations while entity p99 still misses the stricter `<200 ms` goal. |
| 4 | CDC strategy undecided | CDC roadmap can fork between Debezium/Kafka Connect and Python-native connectors with incompatible ops assumptions. | Repo currently ships Debezium config only; CDC work is still treated as future backlog. | Write one ADR for CDC strategy, payload contract, ops model, and follow-up scope across Postgres/MySQL. | 1-2 weeks (planning) | [A04](../../2026-04-Q2-architecture/A04-cdc-strategy-decision.md) | Parallel CDC work can produce incompatible connectors, duplicated maintenance, and unclear deployment requirements. |
| 5 | Helm values contract is implicit | Chart config drift is discovered during staging deploy instead of during validation. | `helm lint` plus hand-maintained staging overrides catch only part of the problem after the fact. | Add formal values schema and validation for embedded YAML blobs such as API keys and tenant config. | 3-5 days | [A05](../../2026-04-Q2-architecture/A05-helm-values-contract-validation.md) | The next required field like `key_id` can break rollout again and waste CI/staging cycles before the root cause is obvious. |
| 6 | Optional extras contract drift | Root extras, subpackage installs, and workflow install lines diverge, causing recurrent missing-dep and over-install regressions. | TA04 documents per-job extras and current quick fixes live in `T06`, `T07`, `T10`. | Define and enforce one dependency-profile matrix for runtime, tests, perf, and integrations. | 4-6 days | [A06](../../2026-04-Q2-architecture/A06-python-extras-contract-rationalization.md) | New jobs and local setups will keep failing on hidden `cloud`/`integrations` assumptions or paying unnecessary install cost. |

## New items added during TA08

- Added item `#6 Optional extras contract drift` from TA04 findings; TA03 and TA05 did not surface additional architectural debt beyond already-tracked quick fixes and hygiene tickets.

## Recommended priority for TA10

- **Fix in next sprint (Q2 2026):** `#2 Query engine mixin host contracts`, `#5 Helm values contract validation`.
- **Do next after CI repair stabilizes:** `#6 Optional extras contract drift`.
- **Defer until planning / external signal:** `#1 SDK package name collision`, `#4 CDC strategy undecided`.
- **Re-baseline before implementation:** `#3 Entity latency optimization plan drift`; do not start with sqlglot-cache-first changes without representative benchmark evidence.
