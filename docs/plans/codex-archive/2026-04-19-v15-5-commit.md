# AgentFlow — v15.5 Commit Step
**Date**: 2026-04-19
**Цель**: закоммитить cleanup из v15.5 тремя логическими коммитами
**Executor**: Codex

## Контекст

Cleanup по `docs/plans/2026-04-19-v15-5-cleanup.md` выполнен, все изменения в working tree. Осталось закоммитить.

## Состояние перед началом

```
M  .gitignore
R  codex-tasks/Archive/*.md -> docs/plans/codex-archive/*
M  docs/release-readiness.md
?? docs/audit-history.md
?? docs/plans/2026-04-19-v15-5-cleanup.md
?? docs/plans/codex-archive/README.md
```

---

## TASK 1 — Commit 1: incident recovery

```bash
git add .gitignore docs/audit-history.md
git commit -m "fix: restore BCG_audit.md classification, add audit history reconstruction

BCG_audit.md was incorrectly classified as a session note in 0bb503c and
deleted during P4A hygiene. The file was never tracked in git, so recovery
was impossible. Added docs/audit-history.md as honest retrospective
reconstruction with explicit 'Reconstructed on 2026-04-19' labelling,
preserving baseline numbers, remediation trail (v8-v15), and before/after
metrics from the plan history."
```

### Verify

```bash
git log -1 --stat | head -20
# Ожидаемо: 2 файла в diff, повестка читается
```

---

## TASK 2 — Commit 2: archive codex-tasks

```bash
# Rename уже staged (как R via git mv), но новый README нужно добавить
git add docs/plans/codex-archive/
git commit -m "chore: archive codex-tasks/ to docs/plans/codex-archive/

Phase 4 closure was executed via a parallel planning system in
codex-tasks/ (orchestrator + P4A/P4B/P4C). Moved to
docs/plans/codex-archive/ for audit trail consolidation. README
documents the P4A deletion incident that motivated v15.5."
```

### Verify

```bash
git log -1 --stat | head -10
# 5 файлов (4 renamed + README)
test ! -d codex-tasks/ && echo "codex-tasks/ removed: OK"
```

---

## TASK 3 — Commit 3: cross-refs + plan trail

```bash
git add docs/release-readiness.md docs/plans/2026-04-19-v15-5-cleanup.md docs/plans/2026-04-19-v15-5-commit.md
git commit -m "docs: update cross-refs to docs/audit-history.md and record v15.5 plans

release-readiness.md cross-refs: BCG_audit.md -> docs/audit-history.md.
Plan trail: 2026-04-19-v15-5-cleanup.md (incident recovery plan),
2026-04-19-v15-5-commit.md (this commit step)."
```

### Verify

```bash
git status --short
# Ожидаемо: пусто (clean tree)
```

---

## TASK 4 — Final verification

```bash
# 1. Clean tree
git status
# Ожидаемо: "nothing to commit, working tree clean"

# 2. History читается
git log --oneline -15

# 3. Full suite (should still be green)
docker compose up -d redis
python -m pytest tests/unit tests/integration tests/sdk --tb=line -q
# Ожидаемо: 543 passed

# 4. Confirm no BCG_audit.md remnants in active docs
grep -rn "BCG_audit" docs/ --exclude-dir=plans 2>&1
# Ожидаемо: пусто (исторические планы в docs/plans/ могут содержать — это OK)
```

---

## Done When

- [ ] 3 коммита созданы с читаемыми сообщениями
- [ ] `git status` clean
- [ ] `git log --oneline -15` читается, история логична
- [ ] Full suite: 543 passed, 0 failed
- [ ] `grep -rn "BCG_audit" docs/ --exclude-dir=plans` — пусто

## Notes

- **НИЧЕГО не push-ить.** Только local commits.
- **Не менять** .gitignore, audit-history.md, release-readiness.md — всё уже в правильном состоянии.
- **Не создавать** новых файлов кроме commit-сообщений.
- Если какой-то `git add` падает с "pathspec did not match" — значит файл уже staged или не существует, просто пропустить.
- Если finding из `grep -rn BCG_audit docs/` ссылается на активный файл (не план) — пометить в отчёте, не править вслепую.
