# AgentFlow — v15 Closeout
**Date**: 2026-04-19
**Цель**: закрыть хвост v15 + финальная сверка документации с BCG
**Executor**: Codex

## Что осталось от v15

Всё основное сделано (`competitive-analysis.md`, `security-audit.md`, `api-reference.md`, landing, fly.toml). Остался только `BCG_audit.md` — в §5 и истории не проставлены новые ✅.

---

## TASK 1 — Обновить `BCG_audit.md` §5

### Изменить таблицу документации

**Было:**
```markdown
| API Reference (beyond OpenAPI) | ❌ | Отсутствует |
| Competitive Analysis | ❌ | Отсутствует |
| Security Audit Report | ❌ | Отсутствует |
```

**Стало:**
```markdown
| API Reference (beyond OpenAPI) | ✅ | `docs/api-reference.md` — narrative с curl/Python/TS примерами на все endpoints (v15) |
| Competitive Analysis | ✅ | `docs/competitive-analysis.md` — 5 прямых конкурентов + 4 заменителя + positioning statement (v15) |
| Security Audit Report | ✅ | `docs/security-audit.md` — 10 разделов, 20 evidence refs, compliance readiness matrix (v15) |
```

### Обновить §8 Phase 4

Пройти по пунктам Phase 4:
- Landing page + documentation site — `✅ v15 (site/index.html + расширение docs/)`
- SDK v2 с retry, circuit breaker, streaming — `⚠️ partial — retry + breaker done (v14), streaming — post-v1.0`
- First 3 paying customers — `❌ post-release, non-technical`
- Public benchmark на production hardware — `❌ требует production infra`

### Добавить в "История исправлений"

```markdown
| v14 | 2026-04-17 | SDK Resilience | Python + TS retry/circuit-breaker, 30+ unit тестов, clean config через configure_resilience() |
| v14-cleanup | 2026-04-17 | Technical debt | Удалён __signature__ хак, честная подпись SDK constructors |
| v15 | 2026-04-19 | Phase 4 GTM (tech part) | Competitive analysis, security audit, API reference, landing page, Fly.io demo config |
```

### Обновить суммарную табличку BCG dimensions

Было (после v13):
| Код | 7.0 → 9.0 |
| Документация | 9.0 → 9.0 |

Стало (после v15):
| Код | 7.0 → 9.0 | (без изменений — Phase 4 не про код) |
| Документация | 9.0 → 9.5 | (+ competitive, security, расширенный API ref) |

### Verify

```bash
# Проверить отметки
grep -c "v15" BCG_audit.md
# Ожидаемо: >= 5 упоминаний v15

# Не осталось ❌ по покрытым пунктам
grep -B1 "❌ | Отсутствует" BCG_audit.md | grep -E "API Reference|Competitive Analysis|Security Audit"
# Ожидаемо: ничего (все ✅)
```

---

## TASK 2 — Финальный verification

Прогнать полный стек проверок, зафиксировать в `docs/release-readiness.md`.

### Команды

```bash
# 1. Full suite (с Redis чтобы не было Redis-flake)
docker compose up -d redis
python -m pytest tests/unit tests/integration tests/sdk --tb=line -q
# Ожидаемо: 542 passed, 0 failed (Redis resolved даёт чистый прогон)

# 2. Bandit
bandit -r src/ sdk/ -f json -o .tmp/bandit-current.json --severity-level medium 2>&1 || true
python scripts/bandit_diff.py .bandit-baseline.json .tmp/bandit-current.json
# exit=0

# 3. Perf gate
python scripts/check_performance.py --baseline docs/benchmark-baseline.json --current docs/benchmark-baseline.json --max-regress 20
# exit=0

# 4. Contract drift
python scripts/generate_contracts.py --check
# exit=0

# 5. Terraform validate
cd infrastructure/terraform
docker run --rm -v "$PWD:/w" -w /w hashicorp/terraform:1.8 init -backend=false >/dev/null 2>&1
docker run --rm -v "$PWD:/w" -w /w hashicorp/terraform:1.8 validate
cd ../..

# 6. Chaos smoke
docker compose -f docker-compose.chaos.yml up -d
python -m pytest tests/chaos/test_chaos_smoke.py -v --timeout=180
docker compose -f docker-compose.chaos.yml down

# 7. HTML landing
python -c "from html.parser import HTMLParser; HTMLParser().feed(open('site/index.html').read()); print('OK')"

# 8. Fly.toml
python -c "import tomllib; tomllib.loads(open('deploy/fly/fly.toml','rb').read().decode()); print('OK')"

# 9. Evidence paths в security doc
python -c "
import re
from pathlib import Path
doc = open('docs/security-audit.md').read()
refs = re.findall(r'\`(src/[^\`\s:]+)', doc)
missing = [r for r in refs if not Path(r).exists()]
assert not missing, f'Missing: {missing}'
print(f'OK — {len(refs)} evidence paths valid')
"

# 10. API reference покрытие
python -c "
import json
spec = json.load(open('docs/openapi.json'))
paths = list(spec['paths'].keys())
doc = open('docs/api-reference.md').read()
missing = [p for p in paths if p not in doc]
assert not missing, f'Missing endpoints: {missing}'
print(f'OK — all {len(paths)} endpoints documented')
"
```

**Actual local note:** the explicit `docker compose -f docker-compose.chaos.yml up -d` step conflicts with the `tests/chaos` session fixture, which starts the stack itself and causes a duplicate bind on port `8474`. After cleanup, direct `python -m pytest tests/chaos/test_chaos_smoke.py -v --timeout=180` passes (`3 passed in 49.10s`).

### Обновить `docs/release-readiness.md`

В секции "Evidence" добавить:
```markdown
- Phase 4 docs: `docs/competitive-analysis.md`, `docs/security-audit.md`, `docs/api-reference.md`
- Landing: `site/index.html`
- Demo deploy: `deploy/fly/`
- Plan trail: + `2026-04-17-v14-cleanup.md`, `2026-04-17-v15-gtm-phase4.md`, `2026-04-19-v15-closeout.md`
```

В "Release Verdict" — поправить если нужно (скорее всего, та же формулировка "technical release-ready", теперь ещё с GTM docs).

---

## TASK 3 — Consolidated report

## v15 Closeout — результат

### BCG_audit.md updates
- §5 API Reference/Competitive/Security: ✅
- §8 Phase 4 items: 8 статусов обновлены; landing/demo/docs закрыты, SDK v2 остаётся partial, customers/benchmark остаются open
- History: v14, v14-cleanup, v15 добавлены

### Full verification
| Check | Result |
|-------|--------|
| pytest (with Redis) | 543 passed, 0 failed |
| bandit diff | exit=0, 0 new findings (`baseline: 1 issues`) |
| perf gate | exit=0 |
| contract drift | exit=0 |
| terraform validate | OK |
| chaos smoke | documented failure for scripted pre-up (`8474` already allocated by fixture); direct pytest: 3 passed in 49.10s |
| HTML landing | OK |
| fly.toml | OK |
| security evidence paths | 20 valid |
| API reference coverage | 6/6 endpoints |

### Overall
RELEASE-READY

### Open items (non-blocking, post-release)
- Phase 1 PMF: customer discovery interviews, pricing model — founder work
- Real Terraform apply: AWS creds + OIDC в GH environments (manual)
- External pen-test (security firm engagement)
- Public benchmark на production hardware (c8g.4xlarge+)
- First 3 paying customers (sales)

---

## Done When

- [x] `BCG_audit.md` §5: 3 строки обновлены ✅ с ссылками на v15 файлы
- [x] `BCG_audit.md` §8 Phase 4: статусы проставлены (landing ✅, SDK v2 ⚠️, customers ❌, benchmark ❌)
- [x] `BCG_audit.md` История исправлений: v14, v14-cleanup, v15 добавлены
- [x] `docs/release-readiness.md` evidence section обновлён
- [x] Все 10 verification команд из Task 2 прошли (или documented failure)
- [x] Отчёт Task 3 консолидирован

## Notes

- **Не добавлять** новых feature'ов. Только closeout.
- Если `test_analytics_logging_is_non_blocking` падает без Redis — это OK, просто пометить в отчёте "requires Redis, passes with docker compose up -d redis".
- Это **последний closeout** для v1.0.0 tech release. После него — только non-Codex работа (PMF, sales, ops).
