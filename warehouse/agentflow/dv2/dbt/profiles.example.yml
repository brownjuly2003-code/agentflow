dv2_marts:
  outputs:
    dev:
      type: clickhouse
      host: clickhouse
      port: 9000
      user: default
      password: demo
      schema: marts
      secure: false
      verify: false
      # 1 on the 8 GB demo host: two concurrent mart builds over the X5-scale
      # bv_order_canonical aggregation exceed the ClickHouse pod budget.
      threads: 1
  target: dev
